// utils/enums.dart

enum PlotState { empty, planted, watered, fertilized, harvested }

enum SimulinType { water, fertilizer, seeding, harvesting, pestControl }

enum CropType { tulip, rose, sunflower }